swagger: '2.0'
info:
  title: 财务云友报账参照 API
  description: Move your app forward with the SSC API
  version: "1.0.0"
# the domain of the service
host: api.yyssc.org
# array of all schemes that your API supports
schemes:
  - http
  - https
# will be prefixed to all paths
basePath: /
produces:
  - application/json

paths:

  /refbase_ctr/queryRefJSON:
    x-swagger-router-controller: refbase_ctr_queryrefjson
    post:
      description: 参照查询接口
      tags:
        - 参照
      consumes:
      - application/json
      produces:
      - application/json
      parameters:
        - required: true
          name: body
          in: body
          description: 查询条件
          schema:
            $ref: '#/definitions/RequestModel'
      responses:
        '200':
          description: Success
          schema:
            $ref: '#/definitions/SuccessResponseModel'
        '404':
          description: Not Found

  /userCenter/queryIdAndNameByCode:
    x-swagger-router-controller: usercenter_queryidandnamebycode
    post:
      description: |
        用户参照查询接口
        这个参照比较另类，不在参照的通用查询接口中
      tags:
        - 用户参照
      consumes:
      - application/json
      produces:
      - application/json
      parameters:
        - required: true
          name: body
          in: body
          description: 查询条件
          schema:
            $ref: '#/definitions/RequestModel'
      responses:
        '200':
          description: Success
          schema:
            $ref: '#/definitions/SuccessResponseModel'
        '404':
          description: Not Found

definitions:

  RequestModel:
    type: object
    required:
    - refCode
    - refType
    - rootName
    properties:
      refCode:
        type: string
        description: 基础档案id，比如`dept`
      refType:
        type: string
        description: 参照的UI类型，比如`tree`
      rootName:
        type: string
        description: 不知道干啥用的，比如`部门`
  SuccessResponseModel:
    type: object
    required:
    - data
    properties:
      data:
        type: array
        description: 参照字段模型
        items:
          $ref: '#/definitions/ReferFieldModel'

  ReferFieldModel:
    type: object
    required:
    - id
    - name
    - code
    properties:
      id:
        type: string
        description: id
      isLeaf:
        type: string
        description: id
      name:
        type: string
        description: id
      pid:
        type: string
        description: id
      code:
        type: string
        description: id
    example:
      
      id: 'E6CB6CBE-C701-48EC-A3EB-C823DF8DBEED'
      isLeaf: 'true'
      name: '服务中心'
      pid: 'FBA1DBB5-24A2-4A78-A4D5-453F7CC46AA6'
      code: '02'

